# RegimeShift report

`RegimeShift_Quant_Research_Report.pdf` is the release-facing institutional research report.

Build it from the editable Python source with:

```powershell
python scripts/build_report.py
```

The PDF uses selectable text, A4 pages, page numbers, and the committed research artefacts. It is a retrospective research report, not an investment recommendation.
